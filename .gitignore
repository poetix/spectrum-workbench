/target

# Third-party conformance test data, fetched by scripts/fetch-testdata.sh.
# Kept out of the tree for licensing reasons — see that script.
/crates/z80/tests/fixtures/

# The 48K ROM, fetched by scripts/fetch-rom.sh, and kept out for the same
# reason: it is Amstrad's, and it is a test input rather than part of this.
/crates/rkw-spectrum/tests/fixtures/

# Per-developer worktrees and session state, not part of the project.
/.claude/
