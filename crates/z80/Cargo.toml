[package]
name = "z80"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "Z80 CPU core with M-cycle-granular bus access"

[dependencies]

[dev-dependencies]
alloc-check = { path = "../alloc-check" }

[lints]
workspace = true
